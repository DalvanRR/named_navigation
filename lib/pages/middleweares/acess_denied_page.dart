import 'package:flutter/material.dart';

class AcessDeniedPage extends StatelessWidget {
  const AcessDeniedPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Acess Denied!'),
      ),
      body: Container(),
    );
  }
}
