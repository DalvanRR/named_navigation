import 'package:flutter/material.dart';

class NotFoundRoutePage extends StatelessWidget {
  const NotFoundRoutePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Not Found Route Page!'),
      ),
      body: Container(),
    );
  }
}
